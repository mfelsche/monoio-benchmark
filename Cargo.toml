[workspace]

members = [
    "monoio-server",
    "tokio-server",
    "glommio-server",
    "async-std-server",

    "client",

    "config"
]

[profile.release]
lto = true
debug = false
overflow-checks = false
